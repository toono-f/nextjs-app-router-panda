import { Header } from "./Header";

const description = ``;

export default {
  component: Header,
  parameters: {
    docs: {
      description: {
        component: description,
      },
    },
  },
};

export const Default = {
  args: {},
};
